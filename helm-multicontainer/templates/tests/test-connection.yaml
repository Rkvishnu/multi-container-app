apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-multicontainer.fullname" . }}-test-connection"
  labels:
    {{- include "helm-multicontainer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-multicontainer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
